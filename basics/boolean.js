// === equality operator
// !== not equal operator
// < less than
// > more than
// <= less/equal
// >= more/equal

/*let temp = 80
let isFreezing = temp <= 31

if (isFreezing) {
    console.log('It is Freezing outside.')
} else {
    console.log('It\'s not freezing outside.')
}*/

//console.log(isFreezing)

let age = 31
/*let isChild = age <= 7
let isSenior = age >= 65*/

//console.log('Your age is ' + age + '. You\'re a senior? - ' + isSenior + '. You\'re a child? - ' + isChild + '.' )

if (age <= 7 || age >= 65) {
    console.log('Your age is ' + age + ' and You\'re getting Discount.')
} else {
    console.log ('You\'re paying full price. We\'re Sorry ;)')
}